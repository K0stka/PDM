import { NextPage } from "next";
import PageTemplate from "@/components/utility/PageTemplate";

const NowAttendingPage: NextPage = async () => {
	return <PageTemplate title="Kde mám teď být?" />;
};

export default NowAttendingPage;
