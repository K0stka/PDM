import { NextPage } from "next";
import PageTemplate from "@/components/utility/PageTemplate";

const ClaimsPage: NextPage = async () => {
	return <PageTemplate title="Volba dílen" />;
};

export default ClaimsPage;
